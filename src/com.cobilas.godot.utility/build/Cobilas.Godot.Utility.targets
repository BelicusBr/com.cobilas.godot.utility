<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<!--<Import Project="Cobilas.Godot.Utility.props"/>-->
	<!--
	<UsingTask
		TaskName="Cobilas.GodotEditor.Utility.Tasks.GodotUtilityTask"
		AssemblyFile="$(CurrentPathPackageFolder)\build\runtime\$(TargetFramework)\com.cobilas.godot.utility.tasks.dll"/>
	-->
	<UsingTask Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'"
		TaskName="Cobilas.GodotEditor.Utility.Tasks.GodotPluginTask"
		AssemblyFile="$(CurrentPathPackageFolder)\build\runtime\$(TargetFramework)\com.cobilas.godot.utility.tasks.dll"/>
    
    <UsingTask Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'"
		TaskName="Cobilas.GodotEditor.Utility.Tasks.GodotPluginTask"
		AssemblyFile="runtime\$(TargetFramework)\com.cobilas.godot.utility.tasks.dll"/>

	<!-- Executar automaticamente depois do build -->
	<!--
	<Target Name="RunCobilasUtilityTask" AfterTargets="Build">

		<GodotUtilityTask ProjectPath="$(MSBuildProjectDirectory)"/>

	</Target>
	-->
	<!--AfterTargets="RunCobilasUtilityTask"-->
	<Target Name="RunCobilasPluginTask" BeforeTargets="BeforeCompile" Condition="'$(_GodotUtilityExecuted)' != 'true'">
		
		<GodotPluginTask ProjectPath="$(MSBuildProjectDirectory)"/>
        <PropertyGroup>
            <_GodotUtilityExecuted>true</_GodotUtilityExecuted>
        </PropertyGroup>
	</Target>

</Project>
